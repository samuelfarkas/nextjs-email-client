// Email list is rendered via @list parallel route slot
// Thread content is rendered via @thread parallel route slot
// This page exists only for route matching
export default function ThreadPage() {
  return null;
}
